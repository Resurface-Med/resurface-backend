// Nothing to see here — this project is an API. A root page keeps `next build`
// happy and gives uptime checks something cheap to hit.
export default function Page() {
  return <main style={{ fontFamily: "system-ui", padding: 40 }}>Resurface API</main>;
}
